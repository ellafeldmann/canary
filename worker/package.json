{
  "name": "canary-subscriptions-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@tursodatabase/serverless": "^0.1.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
